# دليل استكشاف الأخطاء - حفظ مفاتيح API

## المشكلة
عند محاولة حفظ مفتاح API في الإعدادات، يفشل الحفظ.

## التحديثات التي تم تطبيقها

### 1. معالجة الأخطاء في API Route
**الملف:** `app/api/config/route.ts`
- أضفنا try-catch شامل
- الآن نسجل الأخطاء في console
- نرسل رسائل خطأ واضحة للواجهة الأمامية
- نرجع HTTP 500 عند الفشل

### 2. معالجة الأخطاء في المكون
**الملف:** `components/jarvis/provider-card.tsx`
- إضافة console.log عند بدء الحفظ
- تحسين رسائل خطأ الـ toast
- تسجيل الأخطاء بالكامل

### 3. معالجة الأخطاء في الصفحة
**الملف:** `app/settings/page.tsx`
- إضافة try-catch في `patchConfig`
- تسجيل الطلبات والاستجابات
- تسجيل كود الحالة والرسالة

### 4. معالجة الأخطاء في مستوى البيانات
**الملف:** `lib/config.ts`
- إضافة معالجة أخطاء لكل دالة
- معالجة أخطاء Redis
- معالجة أخطاء التشفير
- رسائل خطأ واضحة

## كيفية التصحيح

### الخطوة 1: فتح أدوات المطور
1. افتح التطبيق
2. اضغط على F12 أو Ctrl+Shift+I
3. اذهب إلى tab "Console"

### الخطوة 2: حاول حفظ المفتاح
1. اذهب إلى الإعدادات
2. انقر على مزود الخدمة (مثل OpenRouter)
3. الصق مفتاح API
4. اضغط "حفظ"

### الخطوة 3: اقرأ الرسائل
في console ستجد:
```
[v0] Saving API key for provider: openrouter
[v0] Patching config: {...}
[v0] Config patch failed: 500 POST error message
```

## المشاكل الشائعة وحلولها

### مشكلة: "Failed to save configuration"
**السبب المحتمل:** مشكلة في Redis
**الحل:**
1. تحقق من متغيرات البيئة: `KV_REST_API_URL` و `KV_REST_API_TOKEN`
2. تأكد من الاتصال بـ Upstash Redis

### مشكلة: "Redis timeout"
**السبب المحتمل:** الخادم مشغول
**الحل:**
1. انتظر ثوانٍ قليلة
2. جرّب مرة أخرى

### مشكلة: مفتاح API ممسوح (مثل `sk-1•••••••`)
**السبب:** النظام يخفي المفاتيح تلقائياً لأمان أفضل
**الحل:** هذا طبيعي - المفتاح مخزن بشكل آمن

## اختبار الاتصال

لاختبار ما إذا كان المفتاح يعمل:

1. اضغط على المزود بعد الحفظ
2. قائمة النماذج يجب أن تحمل
3. اختر نموذج
4. اضغط "تحقق" وتحقق من الاتصال

## المتطلبات

✓ `KV_REST_API_URL` - رابط Upstash Redis
✓ `KV_REST_API_TOKEN` - مفتاح Upstash Redis
✓ مفتاح API صحيح من المزود

## آخر تحديث
تم إضافة معالجة أخطاء شاملة لجميع طبقات التطبيق.
